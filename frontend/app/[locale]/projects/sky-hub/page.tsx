import { Wip } from "@/components/Wip";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sky Hub",
};

function SkyHubPage() {
  return <Wip />;
}

export default SkyHubPage;
