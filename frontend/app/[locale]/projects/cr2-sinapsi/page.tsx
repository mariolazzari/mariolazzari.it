import { Wip } from "@/components/Wip";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "CR2 Sinapsi",
};

function Cr2SinapsiPage() {
  return <Wip />;
}

export default Cr2SinapsiPage;
