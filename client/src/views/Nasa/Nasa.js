import React from "react";
// MUI components
import Typography from "@material-ui/core/Typography";

// components
const Nasa = () => {
  return (
    <div>
      <Typography variant="h1">NASA</Typography>
    </div>
  );
};

export default Nasa;
